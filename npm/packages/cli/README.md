# @veeling/cli

Veeling CLI packaged for local frontend project usage.

Install in a frontend repository:

```bash
npm install -D @veeling/cli
```

Invoke the CLI through local binaries:

```bash
npx veeling --version
```

Global installation guidance remains NuGet-first (`dotnet tool install --global veeling`).
